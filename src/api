import axios from 'axios';

const instance = axios.create({
    baseURL: 'http://localhost:8080', // Změň podle svého nastavení backendu
    headers: {
        'Content-Type': 'application/json',
    },
});

export default instance;
